void main(){
  // Basic lambda Function
 /* var add = (int a , int b) => a+b; // Arrow function
  var sub = (int c , int d) => c-d;
  var mul = (int e , int f) => e*f;
  
  print(add(3,4));
  print(sub(5,1));
  print(mul(3,3));*/

  // Using a lambda function to multiply to each number by 2
  // Creating a list
 /* List<int> numbers = [1,2,3,4,5];

  var Mul = numbers.map((number) => number * 2).toList(); // toList used for values in List
  print(Mul);*/

  // * Even number

  /*var isEven = (int number) => number % 2 == 0;
  print(isEven(4)); // output
  print(isEven(7)); // False

  var Even = (int number) => number % 2 == 0;
  print('2 is Even number? : ${Even(2)}');
  print('3 is Even number? : ${Even(3)}');*/



  // Lists lambda function
 /* List<String> names = ['yaseen','ali','basit'];
  names.forEach((name) => print('Hello , $name'));

*/
  // syntax:
 //  (parameter) => expression


/*
  var add = (int a, int b) => a+b;
  print(add(2,4));

  var sub = (int c, int d) => c-d;
  print(sub(4,5));
*/




}